@model IEnumerable<Pie>

<li class="dropdown">
    <a asp-controller="Pie"
       asp-action="List"
       class="dropdown-toggle" data-toggle="dropdown">Weekly Selection<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var pie in Model)
        {
            <li>
                <a asp-controller="Pie" asp-action="Details" asp-route-id="@pie.PieId">@pie.Name</a>
            </li>
        }
    </ul>
</li>